# Agent settings
ANTHROPIC_API_KEY=
ANTHROPIC_BASE_URL=
ANTHROPIC_MODEL=glm-4.7

# Ghidra runner image
RUNNER_IMAGE=ireng-runner

# Function prioritization weights (score = alpha*norm(xrefs) + beta*norm(size))
FUNCTION_PRIORITY_ALPHA=0.7
FUNCTION_PRIORITY_BETA=0.3
